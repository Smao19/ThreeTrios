package provider.model.cell;

/**
 * Represents types of cells where
 *  * HOLE is a hole (i.e no card can play there)
 *  * CARD is a cell that can hold a card.
 */
public enum CellType {
  HOLE, CARD;
}
